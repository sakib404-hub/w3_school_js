//? conditional statements allows us to perform different actions for different conditions
//? conditional statements runs different code depending on te true or false condition

//! ? if - Use if to specify a code block to be executed, if a specified condition is true
//! ? if...else - use else to specify a code block to be executed, if the same condition is false
//! ? if....else if...else -> Use else if to specify a new condition to test, if the first condition is false
//! ? switch case --> Use switch to specify many alternative code blocks to be executed
//! ? ternary ---> Use (? :) (ternary) as a shorthand for if...else

/*if(condition here){
    executionable code
}*/

// if (condition1) {
//   // code to execute if condition1 is true
// } else if (condition2) {
//   // code to execute if the condition1 is false and condition2 is true
// } else {
//   // code to execute if the condition1 is false and condition2 is false
// }

// switch(expression) {
//   case x:
//     // code block
//     break;
//   case y:
//     // code block
//     break;
//   default:
//     // code block
// }
